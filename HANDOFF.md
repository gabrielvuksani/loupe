# loupe handoff

loupe is a self-verifying browser: deterministic design and accessibility detection where AI
only applies the fix. Read `README.md` first for the what and the run commands. Architecture
decisions are in `docs/adr/0001` and in Engram (topic_key `architecture/goldeye-loop`, kept
under the old name on purpose so the history stays linked).

## Where things are
- GitHub: `github.com/gabrielvuksani/loupe` (PRIVATE). `master` is the default branch.
- `master` == `feat/design-mode-feature` == HEAD `00afb25`, all pushed. Tree clean.
- This working tree: `~/orca/workspaces/design-harness/goldeye`. The folder is still named
  `goldeye`; that is cosmetic only (every package, the repo, the npm name, and all code say
  loupe). It is a git worktree, so renaming the folder needs `git worktree move` and would
  break an active shell. Left for safety. The `master` worktree lives at `~/Projects/design-harness`.

## Packages (117 unit tests + real-browser integration, 3 typechecks exit 0)
- `packages/engine`: pure, DOM-free detection and fix computation. Rules: contrast (WCAG with
  an OKLCH hue-preserving fix via culori plus an APCA reading), target-size (44px, inline `<a>`
  exempted per WCAG 2.5.5), large-text, line-length, semantic-tag, type-scale, font-variety,
  font-weights, spacing-scale, color-count, accent-spread, shades-per-color, color-vision
  (saturated pairs distinct to normal sight but confusable under color blindness), plus
  tabindex-order for a positive tabindex that breaks keyboard order. `vision.ts` holds the CVD
  simulation matrices shared by the color rule and the extension overlay. `buildPacket` /
  `packetToMarkdown` build the agent context; the packet now carries a `uniqueSelector`
  (full nth-of-type path) and `box` x/y position so a weak model targets the exact element.
  `captureElement` / `capturePage` are the DOM adapters. `reverifyElement` is the pure loop.
- `packages/connected` (published to npm as `loupe-cli`): the daemon. `loupe serve` = WS bridge
  (:8791) for the browser + MCP over HTTP (http://127.0.0.1:8792/mcp) for agents, sharing one
  selection store, plus `/health`. Real Playwright render, axe-core, Lighthouse. MCP tools:
  loupe_get_selection, loupe_reverify, loupe_score_taste, loupe_analyze_url, loupe_analyze_element.
  Agent dispatch (composeDispatch / runDispatch) carries the user's request. SSRF guard
  (assertRenderableUrl). Optional WS token (`serve --token`). Session map capped at 32.
- `packages/extension` (@loupe/extension): WXT MV3 Lens. Standalone runs the engine in-page;
  axe-core is LAZY-loaded on demand via `entrypoints/axe.ts` + chrome.scripting (content.js is
  71KB, not 654KB). Connected publishes the selection and the user's request over the bridge.
  Side panel has mode/agent pills, project root, optional token, a "what do you want changed?"
  request field, a connection status pill, and the score climb on re-verify. A color-vision
  dropdown filters the live page through the engine's own CVD matrices to simulate
  protanopia/deuteranopia/tritanopia/achromatopsia. Store icons in `public/icon/`
  (placeholder, generated by `scripts/make-icons.mjs`).

## Run / build / test
- `pnpm install`
- `pnpm test` (117 unit, fast)
- `pnpm test:integration` (real browser; 5 pass, gated suites need flags below)
- `pnpm --filter @loupe/extension build`, then load `.output/chrome-mv3` unpacked
- `pnpm --filter loupe-cli serve` (or `node packages/connected/dist/bin.js serve` after build)
- Gated integration flags: `LOUPE_LIVE_PULL=1` (real browser + HTTP MCP pull, proven),
  `LOUPE_LIVE_REALWORLD=1` (renders real sites), `LOUPE_LIVE_DISPATCH=1` (spawns a real agent).
- Never pipe `tsc` or tests through `tail`/`head`; it masks the exit code. Read the count line.

## The loop and send-to-agent
Connected mode, two ways to drive it, both proven end to end in `pull.integration.test.ts`:
1. Type a request in the Lens and hit Send: the bridge spawns your agent (runDispatch) with the
   element context plus your request, and it works autonomously.
2. Leave your agent session open: it calls `loupe_get_selection` and pulls the same context plus
   your request. `get_selection` returns the unique selector, pixel position, role/name,
   source file:line, a cropped screenshot, every computed fix, and the request, in your words.

## Publishing (loupe-cli) and the store
- npm publish FROM THE PACKAGE, not the repo root (root is private + has no version; publishing
  there throws "Cannot read properties of null (reading 'prerelease')"):
  `npm login` then `cd packages/connected && pnpm publish --no-git-checks` (prepack builds the
  bundle, pnpm rewrites the workspace ref). Then `npx loupe-cli serve` works anywhere.
  Verified by installing the packed tarball into a clean dir outside the monorepo and booting.
  Render tools need `npx playwright install chromium` once.
- Chrome Web Store: `pnpm --filter @loupe/extension exec wxt zip` produces
  `.output/loupeextension-0.1.0-chrome.zip`. STILL NEEDS before submission: a designed icon
  (the current `public/icon` set is a placeholder gold circle), a privacy policy (required for
  the `<all_urls>` host permission; the truthful story is local-only, nothing leaves the
  machine), a $5 developer account, and review.

## Verification status (be precise)
- The deterministic loop, the on-page popover, lazy axe in Standalone, the enriched packet, and
  the full pull path (real extension publishes a selection + request, a real MCP-over-HTTP client
  pulls both) all have passing tests. The send-to-agent request is proven e2e.
- The live LLM editing source then re-verifying is gated (`LOUPE_LIVE_DISPATCH`), ran live with
  Claude Code on 2026-06-16, and passed. Codex was blocked by an account limit that day.
- Verified on macOS only; not yet run on Windows.

## Roadmap (the next features, roughly by value)
1. Responsive / multi-viewport checks: DONE 2026-06-16. renderAndAnalyze probes 375/768/1280 and
   the pure `responsiveFindings` rule emits a `responsive-overflow` finding (new `responsive`
   score category) per overflowing width, proven in integration.test. Next within this theme:
   per-breakpoint element collisions and layout shifts beyond pure horizontal overflow.
2. Vision simulation: DONE 2026-06-16. Two parts: a pure `color-vision` engine rule (vision.ts)
   that flags saturated color pairs distinct to normal sight but confusable under
   protanopia/deuteranopia/tritanopia (a two-condition OKLab gate, no false-precision fix), and
   a panel dropdown that filters the live page through the same CVD matrices so what you see is
   what loupe flags. Proven in vision.test.ts and the extension integration test.
3. Keyboard and focus-order auditing: positive tabindex DONE 2026-06-16 (tabindex-order rule in
   analyze.ts, exact remedy in the message, no CSS fix since it is an attribute change; captured
   in both DOM adapters, proven in analyze.test.ts and the render integration). Still open:
   missing focus indicators (needs :focus computed-style capture, which is false-positive prone).
4. "Fix everything" batch dispatch (apply all findings in one send).
5. Stream the agent's diff/progress back into the panel live (currently just a toast).
6. Canonical design-system check: grade against tokens the user authored, not only universal
   invariants. `analyzePage` already infers the page's own system.
7. Hover/focus state checks; show position/target in the on-page popover too.

## Parked (extension scope) and out of scope
- Parked: designed store icon, privacy policy, Windows run, the cosmetic folder rename, asserting
  the screenshot capture path in CI.
- Out of scope unless the user redirects: the greenfield app-maker, the Conductor redesign, cloud
  connected mode, the deeper vm-browser merge. hierarchy-levers is intentionally not a standalone
  rule (covered by type-scale + font-weights + color-count + the agent taste score).

## Gotchas
- The persistent shell can drift cwd into a package dir (it drifted into packages/extension once,
  breaking `pnpm test:integration`). `cd` back to the repo root.
- Backgrounded `serve` smoke tests orphan a node child holding :8791/:8792. Clean with
  `pkill -f "bin.ts serve"` then `lsof -nP -iTCP:8791 -iTCP:8792 -sTCP:LISTEN -t | xargs kill -9`.
- Editor diagnostics sometimes show a stale `@goldeye/engine` or "Cannot find module" after a
  rename or reinstall. Trust the `tsc --noEmit` exit code, not the diagnostic.
- `.gitignore`: its last line had no trailing newline once, so a `printf '...' >>` append mangled
  it and `git add -A` wrongly tracked a file. Append with care.
- Lazy axe needs `host_permissions: ["<all_urls>"]` (the content script already matched
  `<all_urls>`, so no broader data access) and the unlisted `entrypoints/axe.ts`. The extension
  integration test asserts `axe:` findings appear, so it catches a broken injection.
- MV3 service workers need HEADED Chromium in Playwright; headless does not load them.
- The MCP server uses the SDK low-level `Server`; the deprecation notice is cosmetic.
- The engine package main is `src/index.ts`; consumers compile the TS source, and the CLI bundle
  inlines it via esbuild (`build.mjs`), so `@loupe/engine` is a devDependency of loupe-cli.

## Standards the user enforces
- TDD, RED then GREEN, one behavior at a time. The zero-false-positive guard tests
  (`analyze.test.ts` "no false positives", `page.test.ts` clean-page) stay green for every rule.
- DO EVERYTHING YOURSELF. The user explicitly asked not to delegate to subagents this session.
- No em dashes anywhere. No AI slop in prose; write like a human. Verify by running it.
- Commits: short, human, `feat:`/`fix:`/`docs:`/`chore:`, no body, no AI attribution.
- Ship working out of the box, real deps, no lazy fallbacks. Honest reports lead with what is
  NOT done. Save decisions to Engram as you go.
